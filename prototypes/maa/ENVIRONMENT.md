# 真实原型环境与本轮授权

用户已授权继续真实原型，并要求需要人工操作时及时说明。

- 本轮目标：官服 `1-7`，3 次；不吃药、不碎石。
- 游戏环境：Windows 官方桌面端，用户已启动并登录，1-7 已有可用代理。
- MAA 安装包目录版本：`v6.17.5-win-x64`；API 已确认 MaaCore 为 `v6.17.5`。
- 接入方式：核对原生窗口接口，不使用模拟器 ADB 配置。
- 不要求用户先在 MAA GUI 配置任务；原型负责下发受限 `Fight` 参数。

授权覆盖这一轮明确执行，不自动授权额外的停止实验或补刷。若结果不足 3 次或未知，先保留证据并说明，不自动补足。

首轮核对后，用户补充「可能是 MAA 对我的游戏环境不适配，可以再进行一次」。据此增加独立第二轮入口，继续相同三次目标和资源限制；首轮记录保留，不能删除后重放。

第二轮仍失败后，用户决定先自行配置并确认官方 MAA GUI 在本机正常运行，实验当时暂停。后续授权与恢复过程如下；完整结果见 [REPORT.md](REPORT.md)。

后续用户明确表示「已修复配置成功，请继续」，据此恢复实验并准备独立第三轮，仍为三次目标、不吃药、不碎石。已读取 GUI 日志，确认其可以识别主界面和进入公开招募；连接方法及 Core 文件未变化。该观察不代替 `1-7` 的成功计数验证。

2026-09-18 完成后续诊断：系统 DPI 为 `120`（125%），Python 初始未感知 DPI，而官方 GUI 声明 system DPI awareness。原型已对齐该设置，并补载本机 GUI 使用的热更新缓存。前三轮失败的证据全部保留。

修复后累计成功三次：首次成功一局，由旧解析器提前停止；修正可选次数字段解释后，用户启动关联任务完成剩余两局并现场确认一致。当前原型均已退出。本轮授权次数已完成，不自动追加游戏操作；异常路径与 HTTP 合并验证另行安排。

HTTP 合并入口与离线演练随后已准备完成。建议下一轮最多开战两次：首局完成，第二局开战后经 HTTP 停止。该方案仍待用户确认，管理员入口也未执行；当前新增证据全部来自离线回放与替身实验。

用户随后明确选择「暂缓实机，先审阅合并设计」。当前停留在设计审阅阶段，不执行新的主界面探针、MaaCore 或游戏调用。

安装路径、窗口句柄、原始回调、截图与可能包含玩家信息的 Core 日志仅放在本地 `.artifacts/`，提交报告时脱敏。

2026-09-17 管理员侧预检已成功：绑定官方桌面端窗口，取得 `1280×720` 游戏主界面截图，随后销毁实例；没有下发战斗任务。当前普通权限进程截图返回 Windows 错误 5，因此首轮由用户在管理员终端启动。

私有失败即停资源和三次执行参数已通过实际 MaaCore 加载与追加任务检查；该检查没有绑定窗口或启动任务。输入兼容性、实际成功计数、停止及失败路径仍需实机验证。需要用户调整窗口、处理登录/弹窗或核对现场结果时，停下并明确交接。
